@model IEnumerable<backEndCapstone.Models.Background>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.description)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.backgroundAbilities)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.description)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.backgroundAbilities)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.BackgroundId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.BackgroundId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.BackgroundId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
